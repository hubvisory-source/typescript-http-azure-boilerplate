{
  "name": "typescript-azure-http-boilerplate",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest --verbose"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "@types/jest": "^27.0.3",
    "jest": "^27.3.1",
    "stub-azure-function-context": "^2.0.0-alpha.6",
    "ts-jest": "^27.0.7",
    "typescript": "^4.0.0"
  }
}
